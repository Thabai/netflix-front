import React from 'react';
import styled from 'styled-components';
import { PageContainer } from '../../styledComponents';

export const Profile = () => {
    return (
        <PageContainer>
            
        </PageContainer>
    )
}

//update or delete users(lose login status)